# To-Do App (CLI + GUI)

Ova aplikacija omogućuje korisnicima vođenje jednostavne To-Do liste putem:
- **Konzolnog sučelja (CLI)**
- **Grafičkog sučelja (GUI)** napravljenog pomoću `FreeSimpleGUI` biblioteke

---

## Sadržaj projekta

- `cli.py` – konzolna verzija aplikacije (terminal)
- `gui.py` – grafička verzija aplikacije (GUI prozor)
- `functionsGui.py` – zajedničke funkcije za čitanje/spremanje to-do stavki
- `todos.txt` – tekstualna datoteka u kojoj se čuvaju unosi (automatski se stvara)

---

## 🚀 Kako pokrenuti

### ▶️ GUI verzija

```bash
python gui.py
